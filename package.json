{
  "name": "gkd",
  "version": "2.0.0",
  "description": "A templates and dependency manager",
  "license": "MIT",
  "author": "KusStar",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "pub": "yarn build && lerna publish --no-push"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
